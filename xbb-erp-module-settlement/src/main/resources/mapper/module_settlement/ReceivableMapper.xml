<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.settlement.infrastructure.persistence.mapper.ReceivableMapper">

    <sql id="BaseColumns">
        id, corpid, receivable_no, customer_id, source_type, source_invoice_id, opening_batch_id, receivable_date, due_date, amount, written_off_amount, remaining_amount, status, audit_status, remark, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.receivableNo != null and conditionMap.receivableNo != ''">
            and receivable_no = #{conditionMap.receivableNo}
        </if>
        <if test="conditionMap.customerId != null">
            and customer_id = #{conditionMap.customerId}
        </if>
        <if test="conditionMap.sourceType != null and conditionMap.sourceType != ''">
            and source_type = #{conditionMap.sourceType}
        </if>
        <if test="conditionMap.sourceInvoiceId != null">
            and source_invoice_id = #{conditionMap.sourceInvoiceId}
        </if>
        <if test="conditionMap.openingBatchId != null">
            and opening_batch_id = #{conditionMap.openingBatchId}
        </if>
        <if test="conditionMap.receivableDate != null">
            and receivable_date = #{conditionMap.receivableDate}
        </if>
        <if test="conditionMap.dueDate != null">
            and due_date = #{conditionMap.dueDate}
        </if>
        <if test="conditionMap.amount != null">
            and amount = #{conditionMap.amount}
        </if>
        <if test="conditionMap.writtenOffAmount != null">
            and written_off_amount = #{conditionMap.writtenOffAmount}
        </if>
        <if test="conditionMap.remainingAmount != null">
            and remaining_amount = #{conditionMap.remainingAmount}
        </if>
        <if test="conditionMap.status != null">
            and status = #{conditionMap.status}
        </if>
        <if test="conditionMap.creatorId != null and conditionMap.creatorId != ''">
            and creator_id = #{conditionMap.creatorId}
        </if>
        <if test="conditionMap.modifyId != null and conditionMap.modifyId != ''">
            and modify_id = #{conditionMap.modifyId}
        </if>
        <if test="conditionMap.businessSelectKeyword != null and conditionMap.businessSelectKeyword != ''">
            and receivable_no like concat('%', #{conditionMap.businessSelectKeyword}, '%')
        </if>
        <if test="conditionMap.businessSelectOnlyOpen != null and conditionMap.businessSelectOnlyOpen">
            and remaining_amount > 0
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into receivable (
            corpid, receivable_no, customer_id, source_type, source_invoice_id, opening_batch_id, receivable_date, due_date, amount, written_off_amount, remaining_amount, status, audit_status, remark, creator_id, modify_id, del, add_time, update_time
        )
        values (#{corpid}, #{receivableNo}, #{customerId}, #{sourceType}, #{sourceInvoiceId}, #{openingBatchId}, #{receivableDate}, #{dueDate}, #{amount}, #{writtenOffAmount}, #{remainingAmount}, #{status}, #{auditStatus}, #{remark}, #{creatorId}, #{modifyId}, #{del}, #{addTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into receivable (
            corpid, receivable_no, customer_id, source_type, source_invoice_id, opening_batch_id, receivable_date, due_date, amount, written_off_amount, remaining_amount, status, remark, creator_id, modify_id, del, add_time, update_time
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.receivableNo}, #{item.customerId}, #{item.sourceType}, #{item.sourceInvoiceId}, #{item.openingBatchId}, #{item.receivableDate}, #{item.dueDate}, #{item.amount}, #{item.writtenOffAmount}, #{item.remainingAmount}, #{item.status}, #{item.remark}, #{item.creatorId}, #{item.modifyId}, #{item.del}, #{item.addTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="removeById">
        update receivable
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update receivable
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.settlement.infrastructure.persistence.po.ReceivablePO">
        update receivable
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="receivableNo != null">receivable_no = #{receivableNo},</if>
            <if test="customerId != null">customer_id = #{customerId},</if>
            <if test="sourceType != null">source_type = #{sourceType},</if>
            <if test="sourceInvoiceId != null">source_invoice_id = #{sourceInvoiceId},</if>
            <if test="openingBatchId != null">opening_batch_id = #{openingBatchId},</if>
            <if test="receivableDate != null">receivable_date = #{receivableDate},</if>
            <if test="dueDate != null">due_date = #{dueDate},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="writtenOffAmount != null">written_off_amount = #{writtenOffAmount},</if>
            <if test="remainingAmount != null">remaining_amount = #{remainingAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="auditStatus != null">audit_status = #{auditStatus},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.settlement.infrastructure.persistence.po.ReceivablePO">
        select <include refid="BaseColumns"/>
        from receivable
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByIds" resultType="xbb.ai.erp.module.settlement.infrastructure.persistence.po.ReceivablePO">
        select <include refid="BaseColumns"/>
        from receivable
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.settlement.infrastructure.persistence.po.ReceivablePO">
        select <include refid="BaseColumns"/>
        from receivable
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from receivable
        where <include refid="BaseCondition"/>
    </select>
</mapper>
