<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.masterdata.infrastructure.persistence.mapper.CustomerMapper">

    <sql id="BaseColumns">
        id, corpid, customer_code, customer_name, default_contact_id, address, enabled, remark, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.customerCode != null and conditionMap.customerCode != ''">
            and customer_code = #{conditionMap.customerCode}
        </if>
        <if test="conditionMap.customerName != null and conditionMap.customerName != ''">
            and customer_name = #{conditionMap.customerName}
        </if>
        <if test="conditionMap.enabled != null">
            and enabled = #{conditionMap.enabled}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into customer (
            corpid, customer_code, customer_name, default_contact_id, address, enabled, remark, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.customerCode}, #{item.customerName}, #{item.defaultContactId}, #{item.address}, #{item.enabled}, #{item.remark}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update customer
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update customer
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.CustomerPO">
        update customer
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="customerCode != null">customer_code = #{customerCode},</if>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="defaultContactId != null">default_contact_id = #{defaultContactId},</if>
            <if test="address != null">address = #{address},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="updateDefaultContactId">
        update customer
        set default_contact_id = #{defaultContactId}
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.CustomerPO">
        select <include refid="BaseColumns"/>
        from customer
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.CustomerPO">
        select <include refid="BaseColumns"/>
        from customer
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from customer
        where <include refid="BaseCondition"/>
    </select>
</mapper>
