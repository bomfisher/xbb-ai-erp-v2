<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.masterdata.infrastructure.persistence.mapper.ProductSpuMapper">

    <sql id="BaseColumns">
        id, corpid, spu_code, spu_name, category_name, enabled, remark, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.spuCode != null and conditionMap.spuCode != ''">
            and spu_code = #{conditionMap.spuCode}
        </if>
        <if test="conditionMap.spuName != null and conditionMap.spuName != ''">
            and spu_name = #{conditionMap.spuName}
        </if>
        <if test="conditionMap.categoryName != null and conditionMap.categoryName != ''">
            and category_name = #{conditionMap.categoryName}
        </if>
        <if test="conditionMap.enabled != null">
            and enabled = #{conditionMap.enabled}
        </if>
        <if test="conditionMap.remark != null and conditionMap.remark != ''">
            and remark = #{conditionMap.remark}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into product_spu (
            corpid, spu_code, spu_name, category_name, enabled, remark, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.spuCode}, #{item.spuName}, #{item.categoryName}, #{item.enabled}, #{item.remark}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update product_spu
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update product_spu
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.ProductSpuPO">
        update product_spu
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="spuCode != null">spu_code = #{spuCode},</if>
            <if test="spuName != null">spu_name = #{spuName},</if>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="enabled != null">enabled = #{enabled},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.ProductSpuPO">
        select <include refid="BaseColumns"/>
        from product_spu
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.masterdata.infrastructure.persistence.po.ProductSpuPO">
        select <include refid="BaseColumns"/>
        from product_spu
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from product_spu
        where <include refid="BaseCondition"/>
    </select>
</mapper>
