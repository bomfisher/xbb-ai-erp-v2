<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.product.infrastructure.persistence.mapper.ProductBrandMapper">

    <resultMap id="ProductBrandResultMap" type="xbb.ai.erp.module.product.infrastructure.persistence.po.ProductBrandPO">
        <id property="id" column="id"/>
        <result property="corpid" column="corpid"/>
        <result property="brandCode" column="brand_code"/>
        <result property="brandName" column="brand_name"/>
        <result property="sortNo" column="sort_no"/>
        <result property="enableStatus" column="enable_status"/>
        <result property="del" column="del"/>
        <result property="addTime" column="add_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="creatorId" column="creator_id"/>
        <result property="modifyId" column="modify_id"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, corpid, brand_code, brand_name, sort_no,
        enable_status, del, add_time, update_time, creator_id, modify_id
    </sql>

    <sql id="Condition_Where">
        <where>
            del = 0
            <if test="corpid != null and corpid != ''">
                AND corpid = #{corpid}
            </if>
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="brandCode != null and brandCode != ''">
                AND brand_code = #{brandCode}
            </if>
            <if test="brandName != null and brandName != ''">
                AND brand_name LIKE CONCAT('%', #{brandName}, '%')
            </if>
        </where>
    </sql>

    <insert id="insert">
        INSERT INTO product_brand (
            id, corpid, brand_code, brand_name, sort_no,
            enable_status, del, add_time, update_time, creator_id, modify_id
        ) VALUES (
            #{id}, #{corpid}, #{brandCode}, #{brandName}, #{sortNo},
            #{enableStatus}, #{del}, #{addTime}, #{updateTime}, #{creatorId}, #{modifyId}
        )
    </insert>

    <insert id="insertBatch">
        INSERT INTO product_brand (
            id, corpid, brand_code, brand_name, sort_no,
            enable_status, del, add_time, update_time, creator_id, modify_id
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id}, #{item.corpid}, #{item.brandCode}, #{item.brandName}, #{item.sortNo},
                #{item.enableStatus}, #{item.del}, #{item.addTime}, #{item.updateTime}, #{item.creatorId}, #{item.modifyId}
            )
        </foreach>
    </insert>

    <update id="removeById">
        UPDATE product_brand
        SET del = 1, modify_id = #{modifyId}, update_time = #{updateTime}
        WHERE corpid = #{corpid} AND id = #{id} AND del = 0
    </update>

    <update id="removeBatchByIds">
        UPDATE product_brand
        SET del = 1, modify_id = #{modifyId}, update_time = #{updateTime}
        WHERE corpid = #{corpid} AND del = 0
        <if test="ids != null and ids.size() > 0">
            AND id IN
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </update>

    <update id="update">
        UPDATE product_brand
        SET brand_code = #{brandCode},
            brand_name = #{brandName},
            sort_no = #{sortNo},
            enable_status = #{enableStatus},
            modify_id = #{modifyId},
            update_time = #{updateTime}
        WHERE corpid = #{corpid} AND id = #{id} AND del = 0
    </update>

    <select id="findById" resultMap="ProductBrandResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_brand
        WHERE corpid = #{corpid} AND id = #{id} AND del = 0
    </select>

    <select id="findByCondition" resultMap="ProductBrandResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM product_brand
        <include refid="Condition_Where"/>
        <if test="groupByStr != null and groupByStr != ''">
            GROUP BY ${groupByStr}
        </if>
        <choose>
            <when test="orderByStr != null and orderByStr != ''">
                ORDER BY ${orderByStr}
            </when>
            <otherwise>
                ORDER BY id DESC
            </otherwise>
        </choose>
        <if test="offset != null and pageSize != null">
            LIMIT #{offset}, #{pageSize}
        </if>
        <if test="offset == null and pageSize != null">
            LIMIT #{pageSize}
        </if>
    </select>

    <select id="count" resultType="long">
        SELECT COUNT(1)
        FROM product_brand
        <include refid="Condition_Where"/>
    </select>
</mapper>
