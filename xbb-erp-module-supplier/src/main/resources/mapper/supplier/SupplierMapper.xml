<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.supplier.infrastructure.persistence.mapper.SupplierMapper">

    <sql id="BaseColumns">
        id, corpid, supplier_code, supplier_name, supplier_short_name, supplier_category, main_business_category, owner_purchaser_id, owner_purchaser_name_snapshot, biz_status, ref_status, default_contact_id, default_address_id, default_bank_account_id, default_invoice_profile_id, remark, creator_id, modify_id, version, del, add_time, update_time
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.keyword != null and conditionMap.keyword != ''">
            and (
                supplier_code like concat('%', #{conditionMap.keyword}, '%')
                or supplier_name like concat('%', #{conditionMap.keyword}, '%')
            )
        </if>
        <if test="conditionMap.supplierCode != null and conditionMap.supplierCode != ''">
            and supplier_code = #{conditionMap.supplierCode}
        </if>
        <if test="conditionMap.supplierName != null and conditionMap.supplierName != ''">
            and supplier_name = #{conditionMap.supplierName}
        </if>
        <if test="conditionMap.supplierShortName != null and conditionMap.supplierShortName != ''">
            and supplier_short_name = #{conditionMap.supplierShortName}
        </if>
        <if test="conditionMap.supplierCategory != null and conditionMap.supplierCategory != ''">
            and supplier_category = #{conditionMap.supplierCategory}
        </if>
        <if test="conditionMap.mainBusinessCategory != null and conditionMap.mainBusinessCategory != ''">
            and main_business_category = #{conditionMap.mainBusinessCategory}
        </if>
        <if test="conditionMap.ownerPurchaserId != null and conditionMap.ownerPurchaserId != ''">
            and owner_purchaser_id = #{conditionMap.ownerPurchaserId}
        </if>
        <if test="conditionMap.bizStatus != null and conditionMap.bizStatus != ''">
            and biz_status = #{conditionMap.bizStatus}
        </if>
        <if test="conditionMap.refStatus != null and conditionMap.refStatus != ''">
            and ref_status = #{conditionMap.refStatus}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch">
        insert into supplier (
            id, corpid, supplier_code, supplier_name, supplier_short_name, supplier_category, main_business_category, owner_purchaser_id, owner_purchaser_name_snapshot, biz_status, ref_status, default_contact_id, default_address_id, default_bank_account_id, default_invoice_profile_id, remark, creator_id, modify_id, version, del, add_time, update_time
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.corpid}, #{item.supplierCode}, #{item.supplierName}, #{item.supplierShortName}, #{item.supplierCategory}, #{item.mainBusinessCategory}, #{item.ownerPurchaserId}, #{item.ownerPurchaserNameSnapshot}, #{item.bizStatus}, #{item.refStatus}, #{item.defaultContactId}, #{item.defaultAddressId}, #{item.defaultBankAccountId}, #{item.defaultInvoiceProfileId}, #{item.remark}, #{item.creatorId}, #{item.modifyId}, #{item.version}, #{item.del}, #{item.addTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="removeById">
        update supplier
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update supplier
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.SupplierPO">
        update supplier
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="supplierCode != null">supplier_code = #{supplierCode},</if>
            <if test="supplierName != null">supplier_name = #{supplierName},</if>
            <if test="supplierShortName != null">supplier_short_name = #{supplierShortName},</if>
            <if test="supplierCategory != null">supplier_category = #{supplierCategory},</if>
            <if test="mainBusinessCategory != null">main_business_category = #{mainBusinessCategory},</if>
            <if test="ownerPurchaserId != null">owner_purchaser_id = #{ownerPurchaserId},</if>
            <if test="ownerPurchaserNameSnapshot != null">owner_purchaser_name_snapshot = #{ownerPurchaserNameSnapshot},</if>
            <if test="bizStatus != null">biz_status = #{bizStatus},</if>
            <if test="refStatus != null">ref_status = #{refStatus},</if>
            <if test="defaultContactId != null">default_contact_id = #{defaultContactId},</if>
            <if test="defaultAddressId != null">default_address_id = #{defaultAddressId},</if>
            <if test="defaultBankAccountId != null">default_bank_account_id = #{defaultBankAccountId},</if>
            <if test="defaultInvoiceProfileId != null">default_invoice_profile_id = #{defaultInvoiceProfileId},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
            <if test="version != null">version = #{version},</if>
            <if test="del != null">del = #{del},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.SupplierPO">
        select <include refid="BaseColumns"/>
        from supplier
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.SupplierPO">
        select <include refid="BaseColumns"/>
        from supplier
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from supplier
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
    </select>
</mapper>
