<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.customer.infrastructure.persistence.mapper.CustomerMapper">

    <sql id="BaseColumns">
        id, corpid, customer_code, customer_name, customer_short_name, customer_category,
        region_code, owner_sales_id, owner_sales_name_snapshot, biz_status, ref_status,
        default_contact_id, default_address_id, default_bank_account_id, default_invoice_profile_id, remark, creator_id, modify_id, version,
        del, add_time, update_time
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.keyword != null and conditionMap.keyword != ''">
            and (
                customer_code like concat('%', #{conditionMap.keyword}, '%')
                or customer_name like concat('%', #{conditionMap.keyword}, '%')
            )
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" parameterType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerPO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into customer (
            corpid, customer_code, customer_name, customer_short_name, customer_category,
            region_code, owner_sales_id, owner_sales_name_snapshot, biz_status, ref_status,
            default_contact_id, default_address_id, default_bank_account_id, default_invoice_profile_id, remark,
            creator_id, modify_id, version, del, add_time, update_time
        )
        values (
            #{corpid}, #{customerCode}, #{customerName}, #{customerShortName}, #{customerCategory},
            #{regionCode}, #{ownerSalesId}, #{ownerSalesNameSnapshot}, #{bizStatus}, #{refStatus},
            #{defaultContactId}, #{defaultAddressId}, #{defaultBankAccountId}, #{defaultInvoiceProfileId}, #{remark},
            #{creatorId}, #{modifyId}, #{version}, #{del}, #{addTime}, #{updateTime}
        )
    </insert>

    <insert id="insertBatch">
        insert into customer (
            id, corpid, customer_code, customer_name, customer_short_name, customer_category,
            region_code, owner_sales_id, owner_sales_name_snapshot, biz_status, ref_status,
            default_contact_id, default_address_id, default_bank_account_id, default_invoice_profile_id, remark,
            creator_id, modify_id, version, del, add_time, update_time
        )
        values
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id}, #{item.corpid}, #{item.customerCode}, #{item.customerName}, #{item.customerShortName}, #{item.customerCategory},
                #{item.regionCode}, #{item.ownerSalesId}, #{item.ownerSalesNameSnapshot}, #{item.bizStatus}, #{item.refStatus},
                #{item.defaultContactId}, #{item.defaultAddressId}, #{item.defaultBankAccountId}, #{item.defaultInvoiceProfileId}, #{item.remark},
                #{item.creatorId}, #{item.modifyId}, #{item.version}, #{item.del}, #{item.addTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

    <update id="removeById">
        update customer
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update customer
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerPO">
        update customer
        <set>
            <if test="customerCode != null">customer_code = #{customerCode},</if>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="customerShortName != null">customer_short_name = #{customerShortName},</if>
            <if test="customerCategory != null">customer_category = #{customerCategory},</if>
            <if test="regionCode != null">region_code = #{regionCode},</if>
            <if test="ownerSalesId != null">owner_sales_id = #{ownerSalesId},</if>
            <if test="ownerSalesNameSnapshot != null">owner_sales_name_snapshot = #{ownerSalesNameSnapshot},</if>
            <if test="bizStatus != null">biz_status = #{bizStatus},</if>
            <if test="refStatus != null">ref_status = #{refStatus},</if>
            <if test="defaultContactId != null">default_contact_id = #{defaultContactId},</if>
            <if test="defaultAddressId != null">default_address_id = #{defaultAddressId},</if>
            <if test="defaultBankAccountId != null">default_bank_account_id = #{defaultBankAccountId},</if>
            <if test="defaultInvoiceProfileId != null">default_invoice_profile_id = #{defaultInvoiceProfileId},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
            <if test="version != null">version = #{version},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerPO">
        select <include refid="BaseColumns"/>
        from customer
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerPO">
        select <include refid="BaseColumns"/>
        from customer
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>
</mapper>
