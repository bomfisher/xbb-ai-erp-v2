<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.sales.infrastructure.persistence.mapper.SalesOutboundItemMapper">

    <sql id="BaseColumns">
        id, corpid, sales_outbound_id, sales_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, outbound_status, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.salesOutboundId != null">
            and sales_outbound_id = #{conditionMap.salesOutboundId}
        </if>
        <if test="conditionMap.salesOrderItemId != null">
            and sales_order_item_id = #{conditionMap.salesOrderItemId}
        </if>
        <if test="conditionMap.skuId != null">
            and sku_id = #{conditionMap.skuId}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sales_outbound_item (
            corpid, sales_outbound_id, sales_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, outbound_status, creator_id, modify_id
        )
        values (#{corpid}, #{salesOutboundId}, #{salesOrderItemId}, #{skuId}, #{skuName}, #{unitName}, #{qty}, #{unitPrice}, #{amount}, #{costUnit}, #{costAmount}, #{outboundStatus}, #{creatorId}, #{modifyId})
    </insert>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into sales_outbound_item (
            corpid, sales_outbound_id, sales_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, outbound_status, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.salesOutboundId}, #{item.salesOrderItemId}, #{item.skuId}, #{item.skuName}, #{item.unitName}, #{item.qty}, #{item.unitPrice}, #{item.amount}, #{item.costUnit}, #{item.costAmount}, #{item.outboundStatus}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update sales_outbound_item
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update sales_outbound_item
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundItemPO">
        update sales_outbound_item
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="salesOutboundId != null">sales_outbound_id = #{salesOutboundId},</if>
            <if test="salesOrderItemId != null">sales_order_item_id = #{salesOrderItemId},</if>
            <if test="skuId != null">sku_id = #{skuId},</if>
            <if test="skuName != null">sku_name = #{skuName},</if>
            <if test="unitName != null">unit_name = #{unitName},</if>
            <if test="qty != null">qty = #{qty},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="costUnit != null">cost_unit = #{costUnit},</if>
            <if test="costAmount != null">cost_amount = #{costAmount},</if>
            <if test="outboundStatus != null">outbound_status = #{outboundStatus},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundItemPO">
        select <include refid="BaseColumns"/>
        from sales_outbound_item
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundItemPO">
        select <include refid="BaseColumns"/>
        from sales_outbound_item
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sales_outbound_item
        where <include refid="BaseCondition"/>
    </select>
</mapper>
