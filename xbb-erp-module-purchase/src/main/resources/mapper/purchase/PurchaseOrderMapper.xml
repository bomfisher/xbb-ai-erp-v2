<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.purchase.infrastructure.persistence.mapper.PurchaseOrderMapper">

    <sql id="BaseColumns">
        id, corpid, order_no, supplier_id, supplier_name, order_date, expected_date, total_amount, status, audit_status, inbound_status, payment_status, invoice_status, remark, del, add_time, update_time, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.orderNo != null and conditionMap.orderNo != ''">
            and order_no = #{conditionMap.orderNo}
        </if>
        <if test="conditionMap.supplierId != null">
            and supplier_id = #{conditionMap.supplierId}
        </if>
        <if test="conditionMap.orderDate != null">
            and order_date = #{conditionMap.orderDate}
        </if>
        <if test="conditionMap.expectedDate != null">
            and expected_date = #{conditionMap.expectedDate}
        </if>
        <if test="conditionMap.totalAmount != null">
            and total_amount = #{conditionMap.totalAmount}
        </if>
        <if test="conditionMap.status != null and conditionMap.status != ''">
            and status = #{conditionMap.status}
        </if>
        <if test="conditionMap.auditStatus != null">and audit_status = #{conditionMap.auditStatus}</if>
        <if test="conditionMap.inboundStatus != null">and inbound_status = #{conditionMap.inboundStatus}</if>
        <if test="conditionMap.paymentStatus != null">and payment_status = #{conditionMap.paymentStatus}</if>
        <if test="conditionMap.invoiceStatus != null">and invoice_status = #{conditionMap.invoiceStatus}</if>
        <if test="conditionMap.remark != null and conditionMap.remark != ''">
            and remark = #{conditionMap.remark}
        </if>
        <if test="conditionMap.businessSelectKeyword != null and conditionMap.businessSelectKeyword != ''">
            and (order_no like concat('%', #{conditionMap.businessSelectKeyword}, '%')
                or supplier_name like concat('%', #{conditionMap.businessSelectKeyword}, '%'))
        </if>
        <if test="conditionMap.businessSelectAuditStatuses != null and conditionMap.businessSelectAuditStatuses.size() > 0">
            and audit_status in
            <foreach collection="conditionMap.businessSelectAuditStatuses" item="auditStatus" open="(" separator="," close=")">
                #{auditStatus}
            </foreach>
        </if>
        <if test="conditionMap.businessSelectPendingInbound != null and conditionMap.businessSelectPendingInbound">
            and exists (
                select 1 from purchase_order_item item
                where item.purchase_order_id = id
                  and item.corpid = corpid
                  and item.del = 0
                  and item.qty > coalesce(item.inbound_qty, 0)
            )
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_order (
            corpid, order_no, supplier_id, supplier_name, order_date, expected_date, total_amount, status, audit_status, inbound_status, payment_status, invoice_status, remark, del, add_time, update_time, creator_id, modify_id
        )
        values (
            #{corpid}, #{orderNo}, #{supplierId}, #{supplierName}, #{orderDate}, #{expectedDate}, #{totalAmount}, #{status}, #{auditStatus}, #{inboundStatus}, #{paymentStatus}, #{invoiceStatus}, #{remark}, #{del}, #{addTime}, #{updateTime}, #{creatorId}, #{modifyId}
        )
    </insert>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_order (
            corpid, order_no, supplier_id, supplier_name, order_date, expected_date, total_amount, status, audit_status, inbound_status, payment_status, remark, del, add_time, update_time, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.orderNo}, #{item.supplierId}, #{item.supplierName}, #{item.orderDate}, #{item.expectedDate}, #{item.totalAmount}, #{item.status}, #{item.auditStatus}, #{item.inboundStatus}, #{item.paymentStatus}, #{item.invoiceStatus}, #{item.remark}, #{item.del}, #{item.addTime}, #{item.updateTime}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update purchase_order
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update purchase_order
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseOrderPO">
        update purchase_order
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="orderNo != null">order_no = #{orderNo},</if>
            <if test="supplierId != null">supplier_id = #{supplierId},</if>
            <if test="supplierName != null">supplier_name = #{supplierName},</if>
            <if test="orderDate != null">order_date = #{orderDate},</if>
            <if test="expectedDate != null">expected_date = #{expectedDate},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="auditStatus != null">audit_status = #{auditStatus},</if>
            <if test="inboundStatus != null">inbound_status = #{inboundStatus},</if>
            <if test="paymentStatus != null">payment_status = #{paymentStatus},</if>
            <if test="invoiceStatus != null">invoice_status = #{invoiceStatus},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="del != null">del = #{del},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseOrderPO">
        select <include refid="BaseColumns"/>
        from purchase_order
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByIds" resultType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseOrderPO">
        select <include refid="BaseColumns"/>
        from purchase_order
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseOrderPO">
        select <include refid="BaseColumns"/>
        from purchase_order
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from purchase_order
        where <include refid="BaseCondition"/>
    </select>
</mapper>
