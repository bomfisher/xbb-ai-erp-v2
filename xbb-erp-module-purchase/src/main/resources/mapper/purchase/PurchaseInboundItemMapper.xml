<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.purchase.infrastructure.persistence.mapper.PurchaseInboundItemMapper">

    <sql id="BaseColumns">
        id, corpid, purchase_inbound_id, purchase_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.purchaseInboundId != null">
            and purchase_inbound_id = #{conditionMap.purchaseInboundId}
        </if>
        <if test="conditionMap.purchaseOrderItemId != null">
            and purchase_order_item_id = #{conditionMap.purchaseOrderItemId}
        </if>
        <if test="conditionMap.skuId != null">
            and sku_id = #{conditionMap.skuId}
        </if>
        <if test="conditionMap.skuName != null and conditionMap.skuName != ''">
            and sku_name = #{conditionMap.skuName}
        </if>
        <if test="conditionMap.unitName != null and conditionMap.unitName != ''">
            and unit_name = #{conditionMap.unitName}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_inbound_item (
            corpid, purchase_inbound_id, purchase_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, del, add_time, update_time, creator_id, modify_id
        )
        values (#{corpid}, #{purchaseInboundId}, #{purchaseOrderItemId}, #{skuId}, #{skuName}, #{unitName}, #{qty}, #{unitPrice}, #{amount}, #{costUnit}, #{costAmount}, #{del}, #{addTime}, #{updateTime}, #{creatorId}, #{modifyId})
    </insert>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into purchase_inbound_item (
            corpid, purchase_inbound_id, purchase_order_item_id, sku_id, sku_name, unit_name, qty, unit_price, amount, cost_unit, cost_amount, del, add_time, update_time, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.purchaseInboundId}, #{item.purchaseOrderItemId}, #{item.skuId}, #{item.skuName}, #{item.unitName}, #{item.qty}, #{item.unitPrice}, #{item.amount}, #{item.costUnit}, #{item.costAmount}, #{item.del}, #{item.addTime}, #{item.updateTime}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update purchase_inbound_item
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update purchase_inbound_item
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseInboundItemPO">
        update purchase_inbound_item
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="purchaseInboundId != null">purchase_inbound_id = #{purchaseInboundId},</if>
            <if test="purchaseOrderItemId != null">purchase_order_item_id = #{purchaseOrderItemId},</if>
            <if test="skuId != null">sku_id = #{skuId},</if>
            <if test="skuName != null">sku_name = #{skuName},</if>
            <if test="unitName != null">unit_name = #{unitName},</if>
            <if test="qty != null">qty = #{qty},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
            <if test="amount != null">amount = #{amount},</if>
            <if test="costUnit != null">cost_unit = #{costUnit},</if>
            <if test="costAmount != null">cost_amount = #{costAmount},</if>
            update_time = #{updateTime},
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseInboundItemPO">
        select <include refid="BaseColumns"/>
        from purchase_inbound_item
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.purchase.infrastructure.persistence.po.PurchaseInboundItemPO">
        select <include refid="BaseColumns"/>
        from purchase_inbound_item
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from purchase_inbound_item
        where <include refid="BaseCondition"/>
    </select>
</mapper>
