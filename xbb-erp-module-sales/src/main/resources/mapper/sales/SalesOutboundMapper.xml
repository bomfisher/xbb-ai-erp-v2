<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.sales.infrastructure.persistence.mapper.SalesOutboundMapper">

    <sql id="BaseColumns">
        id, corpid, outbound_no, sales_order_id, customer_id, customer_name, warehouse_id, outbound_date, total_amount, status, remark, audit_status, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.outboundNo != null and conditionMap.outboundNo != ''">
            and outbound_no = #{conditionMap.outboundNo}
        </if>
        <if test="conditionMap.salesOrderId != null">
            and sales_order_id = #{conditionMap.salesOrderId}
        </if>
        <if test="conditionMap.customerId != null">
            and customer_id = #{conditionMap.customerId}
        </if>
        <if test="conditionMap.warehouseId != null">
            and warehouse_id = #{conditionMap.warehouseId}
        </if>
        <if test="conditionMap.outboundDate != null">
            and outbound_date = #{conditionMap.outboundDate}
        </if>
        <if test="conditionMap.totalAmount != null">
            and total_amount = #{conditionMap.totalAmount}
        </if>
        <if test="conditionMap.status != null and conditionMap.status != ''">
            and status = #{conditionMap.status}
        </if>
        <if test="conditionMap.auditStatus != null">
            and audit_status = #{conditionMap.auditStatus}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sales_outbound (
            corpid, outbound_no, sales_order_id, customer_id, customer_name, warehouse_id, outbound_date, total_amount, status, remark, audit_status, creator_id, modify_id, del, add_time, update_time
        )
        values (#{corpid}, #{outboundNo}, #{salesOrderId}, #{customerId}, #{customerName}, #{warehouseId}, #{outboundDate}, #{totalAmount}, #{status}, #{remark}, #{auditStatus}, #{creatorId}, #{modifyId}, #{del}, #{addTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into sales_outbound (
            corpid, outbound_no, sales_order_id, customer_id, customer_name, warehouse_id, outbound_date, total_amount, status, remark, audit_status, creator_id, modify_id, del, add_time, update_time
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.outboundNo}, #{item.salesOrderId}, #{item.customerId}, #{item.customerName}, #{item.warehouseId}, #{item.outboundDate}, #{item.totalAmount}, #{item.status}, #{item.remark}, #{item.auditStatus}, #{item.creatorId}, #{item.modifyId}, #{item.del}, #{item.addTime}, #{item.updateTime})
        </foreach>
    </insert>

    <update id="removeById">
        update sales_outbound
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update sales_outbound
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundPO">
        update sales_outbound
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="outboundNo != null">outbound_no = #{outboundNo},</if>
            <if test="salesOrderId != null">sales_order_id = #{salesOrderId},</if>
            <if test="customerId != null">customer_id = #{customerId},</if>
            <if test="customerName != null">customer_name = #{customerName},</if>
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="outboundDate != null">outbound_date = #{outboundDate},</if>
            <if test="totalAmount != null">total_amount = #{totalAmount},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="auditStatus != null">audit_status = #{auditStatus},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundPO">
        select <include refid="BaseColumns"/>
        from sales_outbound
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByIds" resultType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundPO">
        select <include refid="BaseColumns"/>
        from sales_outbound
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.sales.infrastructure.persistence.po.SalesOutboundPO">
        select <include refid="BaseColumns"/>
        from sales_outbound
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sales_outbound
        where <include refid="BaseCondition"/>
    </select>
</mapper>
