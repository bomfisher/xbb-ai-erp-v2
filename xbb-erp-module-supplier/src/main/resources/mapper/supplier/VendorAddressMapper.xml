<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.supplier.infrastructure.persistence.mapper.VendorAddressMapper">

    <sql id="BaseColumns">
        id, corpid, vendor_id, address_type, receiver_name, receiver_mobile, province_code, city_code, district_code, detail_address, postal_code, default_flag, biz_status, creator_id, modify_id, del, add_time, update_time, version
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.vendorId != null">
            and vendor_id = #{conditionMap.vendorId}
        </if>
        <if test="conditionMap.addressType != null and conditionMap.addressType != ''">
            and address_type = #{conditionMap.addressType}
        </if>
        <if test="conditionMap.defaultFlag != null">
            and default_flag = #{conditionMap.defaultFlag}
        </if>
        <if test="conditionMap.bizStatus != null and conditionMap.bizStatus != ''">
            and biz_status = #{conditionMap.bizStatus}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch">
        insert into vendor_address (
            id, corpid, vendor_id, address_type, receiver_name, receiver_mobile, province_code, city_code, district_code, detail_address, postal_code, default_flag, biz_status, creator_id, modify_id, del, add_time, update_time, version
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.corpid}, #{item.vendorId}, #{item.addressType}, #{item.receiverName}, #{item.receiverMobile}, #{item.provinceCode}, #{item.cityCode}, #{item.districtCode}, #{item.detailAddress}, #{item.postalCode}, #{item.defaultFlag}, #{item.bizStatus}, #{item.creatorId}, #{item.modifyId}, #{item.deleted}, #{item.addTime}, #{item.updateTime}, #{item.version})
        </foreach>
    </insert>

    <update id="removeById">
        update vendor_address
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update vendor_address
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.VendorAddressPO">
        update vendor_address
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="vendorId != null">vendor_id = #{vendorId},</if>
            <if test="addressType != null">address_type = #{addressType},</if>
            <if test="receiverName != null">receiver_name = #{receiverName},</if>
            <if test="receiverMobile != null">receiver_mobile = #{receiverMobile},</if>
            <if test="provinceCode != null">province_code = #{provinceCode},</if>
            <if test="cityCode != null">city_code = #{cityCode},</if>
            <if test="districtCode != null">district_code = #{districtCode},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="postalCode != null">postal_code = #{postalCode},</if>
            <if test="defaultFlag != null">default_flag = #{defaultFlag},</if>
            <if test="bizStatus != null">biz_status = #{bizStatus},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
            <if test="deleted != null">del = #{deleted},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="version != null">version = #{version},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.VendorAddressPO">
        select <include refid="BaseColumns"/>
        from vendor_address
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.supplier.infrastructure.persistence.po.VendorAddressPO">
        select <include refid="BaseColumns"/>
        from vendor_address
        where <include refid="BaseCondition"/>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from vendor_address
        where <include refid="BaseCondition"/>
    </select>
</mapper>
