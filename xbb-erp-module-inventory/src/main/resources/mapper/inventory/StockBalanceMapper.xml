<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.inventory.infrastructure.persistence.mapper.StockBalanceMapper">

    <sql id="BaseColumns">
        id, corpid, warehouse_id, sku_id, qty, locked_qty, available_qty, total_cost, unit_cost, version, del, add_time, update_time, creator_id, modify_id
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0

    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into stock_balance (
            corpid, warehouse_id, sku_id, qty, locked_qty, available_qty, total_cost, unit_cost, version, del, add_time, update_time, creator_id, modify_id
        )
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.corpid}, #{item.warehouseId}, #{item.skuId}, #{item.qty}, #{item.lockedQty}, #{item.availableQty}, #{item.totalCost}, #{item.unitCost}, #{item.version}, #{item.del}, #{item.addTime}, #{item.updateTime}, #{item.creatorId}, #{item.modifyId})
        </foreach>
    </insert>

    <update id="removeById">
        update stock_balance
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update stock_balance
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.inventory.infrastructure.persistence.po.StockBalancePO">
        update stock_balance
        <set>
            <if test="corpid != null">corpid = #{corpid},</if>
            <if test="warehouseId != null">warehouse_id = #{warehouseId},</if>
            <if test="skuId != null">sku_id = #{skuId},</if>
            <if test="qty != null">qty = #{qty},</if>
            <if test="lockedQty != null">locked_qty = #{lockedQty},</if>
            <if test="availableQty != null">available_qty = #{availableQty},</if>
            <if test="totalCost != null">total_cost = #{totalCost},</if>
            <if test="unitCost != null">unit_cost = #{unitCost},</if>
            <if test="version != null">version = #{version},</if>
            <if test="del != null">del = #{del},</if>
            <if test="addTime != null">add_time = #{addTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.inventory.infrastructure.persistence.po.StockBalancePO">
        select <include refid="BaseColumns"/>
        from stock_balance
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByWarehouseAndSku" resultType="xbb.ai.erp.module.inventory.infrastructure.persistence.po.StockBalancePO">
        select <include refid="BaseColumns"/>
        from stock_balance
        where corpid = #{corpid} and warehouse_id = #{warehouseId} and sku_id = #{skuId} and del = 0
    </select>

    <select id="findByWarehouseAndSkuPairs" resultType="xbb.ai.erp.module.inventory.infrastructure.persistence.po.StockBalancePO">
        select <include refid="BaseColumns"/> from stock_balance
        where corpid = #{corpid} and del = 0 and (warehouse_id, sku_id) in
        <foreach collection="stockKeys" item="key" open="(" separator="," close=")">
            (#{key.warehouseId}, #{key.skuId})
        </foreach>
    </select>

    <update id="updateWithVersion">
        update stock_balance set qty = #{po.qty}, locked_qty = #{po.lockedQty}, available_qty = #{po.availableQty},
            total_cost = #{po.totalCost}, unit_cost = #{po.unitCost}, version = #{po.version},
            update_time = #{po.updateTime}, modify_id = #{po.modifyId}
        where corpid = #{po.corpid} and id = #{po.id} and version = #{expectedVersion} and del = 0
    </update>

    <select id="findByCondition" resultType="xbb.ai.erp.module.inventory.infrastructure.persistence.po.StockBalancePO">
        select <include refid="BaseColumns"/>
        from stock_balance
        where <include refid="BaseCondition"/>
        <if test="conditionMap.conditions != null and conditionMap.conditions.size() > 0">
            <foreach collection="conditionMap.conditions" item="c">
                <include refid="xbb.ai.erp.module.common.application.filter.CommonListFilterMapper.dynamicCondition"/>
            </foreach>
        </if>
        <include refid="QueryTail"/>
    </select>

    <select id="count" resultType="java.lang.Long">
        select count(1)
        from stock_balance
        where <include refid="BaseCondition"/>
    </select>
</mapper>
