<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xbb.ai.erp.module.customer.infrastructure.persistence.mapper.CustomerInvoiceProfileMapper">

    <sql id="BaseColumns">
        id, corpid, customer_id, invoice_title, tax_no, address_phone, bank_name, bank_account_no,
        default_flag, biz_status, remark, creator_id, modify_id, version, del, add_time, update_time
    </sql>

    <sql id="BaseCondition">
        corpid = #{conditionMap.corpid}
        and del = 0
        <if test="conditionMap.id != null">
            and id = #{conditionMap.id}
        </if>
        <if test="conditionMap.customerId != null">
            and customer_id = #{conditionMap.customerId}
        </if>
        <if test="conditionMap.invoiceTitle != null and conditionMap.invoiceTitle != ''">
            and invoice_title like concat('%', #{conditionMap.invoiceTitle}, '%')
        </if>
        <if test="conditionMap.taxNo != null and conditionMap.taxNo != ''">
            and tax_no = #{conditionMap.taxNo}
        </if>
        <if test="conditionMap.bizStatus != null and conditionMap.bizStatus != ''">
            and biz_status = #{conditionMap.bizStatus}
        </if>
        <if test="conditionMap.defaultFlag != null">
            and default_flag = #{conditionMap.defaultFlag}
        </if>
    </sql>

    <sql id="QueryTail">
        <if test="conditionMap.groupByStr != null and conditionMap.groupByStr != ''">
            group by ${conditionMap.groupByStr}
        </if>
        <if test="conditionMap.orderByStr != null and conditionMap.orderByStr != ''">
            order by ${conditionMap.orderByStr}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset != null">
            limit #{conditionMap.offset}, #{conditionMap.pageSize}
        </if>
        <if test="conditionMap.pageSize != null and conditionMap.pageSize > 0 and conditionMap.offset == null">
            limit #{conditionMap.pageSize}
        </if>
    </sql>

    <insert id="insertBatch">
        insert into customer_invoice_profile (
            id, corpid, customer_id, invoice_title, tax_no, address_phone, bank_name, bank_account_no,
            default_flag, biz_status, remark, creator_id, modify_id, version, del, add_time, update_time
        )
        values
        <foreach collection="list" item="item" separator=",">
            (
                #{item.id}, #{item.corpid}, #{item.customerId}, #{item.invoiceTitle}, #{item.taxNo}, #{item.addressPhone}, #{item.bankName}, #{item.bankAccountNo},
                #{item.defaultFlag}, #{item.bizStatus}, #{item.remark}, #{item.creatorId}, #{item.modifyId}, #{item.version}, #{item.del}, #{item.addTime}, #{item.updateTime}
            )
        </foreach>
    </insert>

    <update id="removeById">
        update customer_invoice_profile
        set del = 1
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <update id="removeBatchByIds">
        update customer_invoice_profile
        set del = 1
        where corpid = #{corpid}
          and del = 0
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <update id="update" parameterType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerInvoiceProfilePO">
        update customer_invoice_profile
        <set>
            <if test="customerId != null">customer_id = #{customerId},</if>
            <if test="invoiceTitle != null">invoice_title = #{invoiceTitle},</if>
            <if test="taxNo != null">tax_no = #{taxNo},</if>
            <if test="addressPhone != null">address_phone = #{addressPhone},</if>
            <if test="bankName != null">bank_name = #{bankName},</if>
            <if test="bankAccountNo != null">bank_account_no = #{bankAccountNo},</if>
            <if test="defaultFlag != null">default_flag = #{defaultFlag},</if>
            <if test="bizStatus != null">biz_status = #{bizStatus},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="creatorId != null">creator_id = #{creatorId},</if>
            <if test="modifyId != null">modify_id = #{modifyId},</if>
            <if test="version != null">version = #{version},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </update>

    <select id="findById" resultType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerInvoiceProfilePO">
        select <include refid="BaseColumns"/>
        from customer_invoice_profile
        where corpid = #{corpid}
          and id = #{id}
          and del = 0
    </select>

    <select id="findByCondition" resultType="xbb.ai.erp.module.customer.infrastructure.persistence.po.CustomerInvoiceProfilePO">
        select <include refid="BaseColumns"/>
        from customer_invoice_profile
        where <include refid="BaseCondition"/>
        <include refid="QueryTail"/>
    </select>
</mapper>
